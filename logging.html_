<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>로그 기록</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center p-8">
    <!-- 'relative' 클래스를 추가하여 버튼을 이 컨테이너 내부에 위치시킵니다 -->
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-4xl relative">

        <!-- '처음으로 돌아가기' 버튼 추가 -->
        <a href="/" class="absolute top-4 left-4 bg-gray-500 text-white font-semibold py-2 px-4 rounded-md shadow-md hover:bg-gray-600 transition duration-300 text-sm">
            &larr; 처음으로
        </a>

        <h1 class="text-3xl font-bold text-center text-gray-800 mb-8">저장된 로그 목록</h1>
        <div class="overflow-x-auto">
            <table class="min-w-full bg-white border border-gray-200 rounded-lg">
                <thead>
                    <tr class="bg-gray-100 text-left text-sm font-semibold text-gray-600">
                        <th class="py-3 px-4 border-b">ID</th>
                        <th class="py-3 px-4 border-b">입력값</th>
                        <th class="py-3 px-4 border-b">IP 주소</th>
                        <th class="py-3 px-4 border-b">시간</th>
                    </tr>
                </thead>
                <tbody id="logging-table-body">
                    <tr>
                        <td colspan="4" class="py-3 px-4 text-center text-gray-500">로그를 불러오는 중...</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            fetch('/get_logs.php')
                .then(response => {
                    if (!response.ok) {
                        throw new Error('네트워크 응답 오류');
                    }
                    return response.json();
                })
                .then(data => {
                    const tableBody = document.getElementById('logging-table-body');
                    tableBody.innerHTML = ''; // 기존 로딩 메시지 제거
                    if (data.length > 0) {
                        data.forEach(log => {
                            const row = `
                                <tr class="hover:bg-gray-50 transition-colors">
                                    <td class="py-3 px-4 border-b text-gray-700">${log.id}</td>
                                    <td class="py-3 px-4 border-b text-gray-700">${log.name}</td>
                                    <td class="py-3 px-4 border-b text-gray-700">${log.ip_address}</td>
                                    <td class="py-3 px-4 border-b text-gray-700">${log.log_time}</td>
                                </tr>
                            `;
                            tableBody.innerHTML += row;
                        });
                    } else {
                        tableBody.innerHTML = `<tr><td colspan="4" class="py-3 px-4 text-center text-gray-500">저장된 로그가 없습니다.</td></tr>`;
                    }
                })
                .catch(error => {
                    console.error('로그를 가져오는 중 오류 발생:', error);
                    document.getElementById('logging-table-body').innerHTML = `<tr><td colspan="4" class="py-3 px-4 text-center text-red-500">로그 로딩 중 오류가 발생했습니다.</td></tr>`;
                });
        });
    </script>
</body>
</html>
